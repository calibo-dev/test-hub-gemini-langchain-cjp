name: country_agent_workflow
description: Metadata-driven query workflow for the ak_country Snowflake table

stages:
  - metadata_fetching_agent
  - sql_generator_agent
  - validator_agent
  - query_execution_agent
  - response_generation_agent

execution:
  mode: sequential
  save_output: false
